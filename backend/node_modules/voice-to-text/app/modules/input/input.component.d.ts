import { OnInit, EventEmitter } from '@angular/core';
import { SpeechRecognitionService } from '../../services/speechservice';
export declare class InputComponent implements OnInit {
    private speech;
    search_text: string;
    blink: boolean;
    spokenText: EventEmitter<string>;
    error: EventEmitter<string>;
    showInput: boolean;
    constructor(speech: SpeechRecognitionService);
    ngOnInit(): void;
    triggerMike(): void;
    search(): void;
}
